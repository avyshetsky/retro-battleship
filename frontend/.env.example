# Base URL of the Battleship backend API.
# Leave empty to run the game fully client-side ("LOCAL AI MODE").
# Example for local dev against the backend:
#   VITE_API_BASE_URL=http://localhost:8000
VITE_API_BASE_URL=
